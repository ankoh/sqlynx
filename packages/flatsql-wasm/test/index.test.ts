// import * as flatsql from '../lib';
// import path from 'path';
// import fs from 'fs';

import "@jest/globals";

beforeAll(async () => {});

afterAll(async () => {});

describe('add', () => {
    it('should return 2 when it gives 1,1', () => {
        expect(1 + 1).toBe(2)
    })
})
