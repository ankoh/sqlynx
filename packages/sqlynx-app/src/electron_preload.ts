// import { contextBridge, ipcRenderer } from 'electron';
// import { createIPCBackendBridge } from './backend/ipc_bridge_to_backend';
//
// contextBridge.exposeInMainWorld('backend', createIPCBackendBridge(ipcRenderer));
