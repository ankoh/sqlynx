name: Pull Request

on:
    pull_request:
        branches:
            - 'main'

jobs:
    build:
        name: Build
        uses: ./.github/workflows/build.yml
        with:
            sha: github.event.inputs.sha
        secrets: inherit
